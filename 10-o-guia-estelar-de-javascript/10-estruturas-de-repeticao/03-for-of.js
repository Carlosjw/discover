// for... of: loop em arrays e strings

let name = 'Carlos';
let names = ['Saymon', 'Solange', 'Carlos'];

// na string
for(let char of name) {
    console.log(char);
}

// no array
for(let nome of names){
    console.log(nome);
}

