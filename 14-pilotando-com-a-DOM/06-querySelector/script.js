// querySelector()

const element = document.querySelector('.one');
// Pega o primeiro elemento que encontrar na página. Pode pegar pelo atributo também. Ex: querySelector('[src]') vai pegas os elementos que tenham o atributo src
 
console.log(element);